__pycache__/
*.py[cod]
*.egg-info/
*.o
*.so
*.vcd
build/
work-obj*.cf
isomorph-*.tar.gz

# internal planning, kept on disk and out of the repository
ROADMAP.md
PLAN.txt
SIM_PLAN.txt
IFACE_PLAN.txt
BUILDOUT.md
PARAMETERS.md
DATAFLOW.md
SPEC.txt
HOUSE_STYLE.md

# the test suite is kept on disk and out of the repository. It is
# written against designs invented for it, but a test is where a real
# design gets used as an example and the names in one are not mine to
# publish
tests/
