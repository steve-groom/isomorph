__pycache__/
*.py[cod]
*.egg-info/
*.o
*.so
*.vcd
build/
work-obj*.cf
isomorph-*.tar.gz

# internal planning, kept on disk and out of the repository
ROADMAP.md
PLAN.txt
SIM_PLAN.txt
IFACE_PLAN.txt
BUILDOUT.md
PARAMETERS.md
DATAFLOW.md
SPEC.txt
HOUSE_STYLE.md
examples/house_style.py

# The test suite is published. The designs it runs on were invented
# for it, bar one: the processor core under tests/cores/riscv is not
# mine to publish, so it stays on disk with the two tests that use it.
tests/cores/riscv
tests/test_cpu_sim.py
tests/test_cpu_verilator.py
